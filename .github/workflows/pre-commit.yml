name: Pre-commit Checks

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  treelint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      
    - name: Cache cargo dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        
    - name: Build treelint
      run: cargo build --bin treelint
      
    - name: Run treelint on codebase
      run: ./target/debug/treelint