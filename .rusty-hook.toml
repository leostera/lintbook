[hooks]
pre-commit = """
# Get list of staged files and run treelint on them
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM)
if [ -n "$STAGED_FILES" ]; then
    cargo run -p treelint-cli -- check $STAGED_FILES
fi
"""## Pre-commit Hook
