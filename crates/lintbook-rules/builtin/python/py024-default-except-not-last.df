node(Try, "try_statement", _, _, _, _), child(Try, BareExcept, BareIndex), child(Try, Node, LaterIndex), BareIndex < LaterIndex, node(BareExcept, "except_clause", _, _, _, _), trimmedText(BareExcept, BareText), matchesRegex(BareText, "^except\s*:"), node(Node, "except_clause", _, _, _, _);
node(Try, "try_statement", _, _, _, _), child(Try, BroadExcept, BroadIndex), child(Try, Node, LaterIndex), BroadIndex < LaterIndex, node(BroadExcept, "except_clause", _, _, _, _), trimmedText(BroadExcept, BroadText), matchesRegex(BroadText, "^except\s+Exception\b"), node(Node, "except_clause", _, _, _, _), trimmedText(Node, LaterText), notMatchesRegex(LaterText, "^except\s*:"), notMatchesRegex(LaterText, "^except\s+Exception\b")
